<Project>
	<PropertyGroup>
		<VersionPrefix>1.0.0</VersionPrefix>
    <VersionSuffix>.</VersionSuffix>
    <VersionSuffixVersion>5</VersionSuffixVersion>
		<Version>$(VersionPrefix)$(VersionSuffix)$(VersionSuffixVersion)</Version>
		<AssemblyVersion>$(VersionPrefix).5</AssemblyVersion>
		<FileVersion>$(VersionPrefix).$(VersionSuffixVersion)</FileVersion>
	</PropertyGroup>
</Project>